#pragma once

#include "complex.h"
#include "field.h"
#include "matriz.h"

namespace math
{
	template <typename T>
	complex<T> simpson2d(field<T> f)
	{
		matriz<T> pond(128, 128);

		for(int i = 0; i<128; i++)
		{
			for (int j = 0; j<128; j++)
			{
				
			}
		}
	}
}